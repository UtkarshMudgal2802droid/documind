name: Backend CI 
on: 
  pull_request: 
    branches: [ main ] 
jobs: 
  test: 
    runs-on: ubuntu-latest 
    steps: 
    - uses: actions/checkout@v3 
    - name: Set up Python 
      uses: actions/setup-python@v4 
      with: 
        python-version: '3.10' 
    - name: Install dependencies 
      run: pip install -r fastapi-ai-worker/requirements.txt pytest httpx 
    - name: Run Tests 
      env: 
        PROJECT_NAME: "DocuMind-Test" 
        DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/test_db" 
        KAFKA_BROKER_URL: "localhost:9092" 
        JWT_SECRET_KEY: "ci_test_secret_key_12345" 
        ADMIN_USERNAME: "utkarsh_admin" 
        ADMIN_PASSWORD: "backend_auth_2026" 
        PYTHONPATH: "." 
      run: cd fastapi-ai-worker && pytest app/tests/ 
